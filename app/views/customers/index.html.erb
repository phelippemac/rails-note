<p id="notice"><%= notice %></p>
<div class="col-lg-12 table-responsive">
    <div class="row title">
        <h1>Clientes</h1>
        <%= link_to new_customer_path, {class: 'btn btn-02 btn-lg padd'}  do %>
        <i class="fa fa-plus"></i> Novo Cliente
        <% end %>
    </div>

    <table class="table table-responsive font-change">
        <thead class="thead-dark">
            <tr>
                <th>Nome</th>
                <th>E-mail</th>
                <th>Telefone</th>
                <th colspan=" 3">
                <%= link_to "Relatórios", customers_download_path(@customers, format: 'pdf'), method: :post, class: 'btn btn-warning btn-lg', style: 'width: 20rem' %>
                </th>
            </tr>
        </thead>

        <tbody>
            <% @customers.each do |customer| %>
            <tr>
                <td><%= customer.name %></td>
                <td><%= customer.email %></td>
                <td><%= customer.phone %></td>
                <td><%= link_to 'Mostrar', customer, class: 'btn btn-primary' %></td>
                <td><%= link_to 'Editar', edit_customer_path(customer), class: 'btn btn-warning' %></td>
                <td><%= link_to 'Deletar', customer, method: :delete, class: 'btn btn-danger' ,data: { confirm: 'Tem Certeza?' } %></td>
            </tr>
            <% end %>
        </tbody>
    </table>
    <%= paginate @customers %>
</div>
<br>